함수는 소문자와 언더바 >> 각 언어에 대한 표기법을 지킬 것

FrontNode << 파이썬에서 사용하는 

동적할당 실패했을 때의 예외처리가 필요함 >> 시스템에서 가져다 쓰는 함수에 대한 예외처리가 필수적

변수는 역할을 나타내는 풀네임으로 작성할 것

함수의 리턴 값에 따른 처리가 용이하게 코드를 작성할 것

init : 메모리 할당
exit : 메모리 해제

각 함수의 중첩적인 역할을 없앨 것

폴더 내에서 사용하는 함수의 경우 static을 사용하고 이름은 _로 시작하도록 작성(가독성 업)


C 및 C++ 
C 및 C++ 에서 키워드 와 표준 라이브러리 식별자 는 대부분 소문자입니다. C 표준 라이브러리 에서는 축약된 이름이 가장 일반적이며(예: isalnum문자가 숫자인지 여부를 테스트하는 함수의 경우) C++ 표준 라이브러리 는 종종 밑줄을 단어 구분 기호로 사용합니다(예: out_of_range). 매크로 를 나타내는 식별자는 규칙에 따라 대문자와 밑줄만 사용하여 작성됩니다(이는 상수에 대해 모두 대문자 식별자를 사용하는 많은 프로그래밍 언어의 규칙과 관련이 있습니다). 이중 밑줄을 포함하거나 밑줄과 대문자로 시작하는 이름은 구현을 위해 예약되어 있습니다(컴파일러 , 표준 라이브러리 ) 및 사용해서는 안 됩니다(예: __reserved또는 _Reserved). [19] [20] 이것은 표면적으로 stropping 과 유사 하지만 의미론은 다릅니다. 밑줄은 문자를 인용하는 것이 아니라 식별자 값의 일부입니다(스트로핑과 같이): __foois __foo(예약된) 값, 아닙니다 foo(그러나 다른 네임스페이스에 있음).


